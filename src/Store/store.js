
// import { create } from 'zustand'

// import {devtools,persist} from 'zustand/middleware'

// const useStore = create(persist(devtools((set) => ({
//    mode: "dark",
//   toggle:()=>set(() => (mode === "dark" ? mode="light" : mode="dark")),
  
// })),{name:"Store"}));



// export default useStore;